import 'package:flutter/material.dart';

class MovieDetail extends StatelessWidget {
  final String movieCd;
  const MovieDetail({super.key, required this.movieCd});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("ㅎㅇ"),
    );
  }
}